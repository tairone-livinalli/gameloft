import styled from 'styled-components/native';

const Container = styled.View`
  flex: 1;
`;

const FavoritesList = styled.FlatList`
  background-color: #000;
  padding: 20px;
`;

export { Container, FavoritesList };
